using System;
using System.Collections.Generic;
using System.Globalization;
using System.IO;
using Newtonsoft.Json;
using SAModel;

namespace SplitTools
{
	public static class StructConversion
	{
		public enum TextType
		{
			CStructs,
			NJA,
			JSON
		}

		/// <summary>
		/// Exports a single level, model or animation file as text.
		/// </summary>
		/// <param name="source">Source pathname.</param>
		/// <param name="type">Type of text conversion.</param>
		/// <param name="destination">Destination pathname. Leave blank to export in the same folder with a swapped extension.</param>
		/// <param name="basicDX">Use the SADX2004 format for Basic models.</param>
		public static void ConvertFileToText(string source, TextType type, string destination = "", bool basicDX = true, bool overwrite = true, bool isNinja2 = false, bool includetls = false)
		{
			var outext = ".c";
			var extension = Path.GetExtension(source);
			switch (extension.ToLowerInvariant())
			{
				case ".sa2lvl":
				case ".sa1lvl":
					if (type == TextType.CStructs || type == TextType.NJA)
					{
						if (destination == "")
						{
							destination = Path.Combine(Path.GetDirectoryName(source), Path.GetFileNameWithoutExtension(source) + outext);
						}
						if (!overwrite && File.Exists(destination))
						{
							while (File.Exists(destination))
							{
								destination = destination = Path.Combine(Path.GetDirectoryName(destination), Path.GetFileNameWithoutExtension(destination) + "_" + outext);
							}
						}
						var land = LandTable.LoadFromFile(source);
						var labels = new List<string> { land.Name };
						var fmt = land.Format;
						using (var sw = File.CreateText(destination))
						{
							if (type == TextType.CStructs)
							{
								sw.Write("/* Sonic Adventure ");
								switch (land.Format)
								{
									case LandTableFormat.SA1:
									case LandTableFormat.SADX:
										if (basicDX)
										{
											sw.Write("DX");
											fmt = LandTableFormat.SADX;
										}
										else
										{
											sw.Write("1");
											fmt = LandTableFormat.SA1;
										}
										break;
									case LandTableFormat.SA2:
										sw.Write("2");
										fmt = LandTableFormat.SA2;
										break;
									case LandTableFormat.SA2B:
										sw.Write("2 Battle");
										fmt = LandTableFormat.SA2B;
										break;
								}
								sw.WriteLine(" LandTable");
								sw.WriteLine(" * ");
								sw.WriteLine(" * Generated by DataToolbox");
								sw.WriteLine(" * ");
								if (!string.IsNullOrEmpty(land.Description))
								{
									sw.Write(" * Description: ");
									sw.WriteLine(land.Description);
									sw.WriteLine(" * ");
								}
								if (!string.IsNullOrEmpty(land.Author))
								{
									sw.Write(" * Author: ");
									sw.WriteLine(land.Author);
									sw.WriteLine(" * ");
								}
								sw.WriteLine(" */");
								sw.WriteLine();
							}
							land.ToStructVariables(sw, fmt, labels, null, type == TextType.NJA);
							sw.Flush();
							sw.Close();
						}
					}
					break;
				case ".sa1mdl":
				case ".sa2mdl":
				case ".sa2bmdl":
					var modelFile = new ModelFile(source);
					var model = modelFile.Model;
					var animations = new List<NJS_MOTION>(modelFile.Animations);
					if (type == TextType.CStructs)
					{
						outext = ".c";
						if (destination == "")
						{
							destination = Path.Combine(Path.GetDirectoryName(source), Path.GetFileNameWithoutExtension(source) + outext);
						}
						if (!overwrite && File.Exists(destination))
						{
							while (File.Exists(destination))
							{
								destination = destination = Path.Combine(Path.GetDirectoryName(destination), Path.GetFileNameWithoutExtension(destination) + "_" + outext);
							}
						}
						using (var sw = File.CreateText(destination))
						{
							sw.Write("/* NINJA ");
							switch (modelFile.Format)
							{
								case ModelFormat.Basic:
								case ModelFormat.BasicDX:
									if (basicDX)
									{
										sw.Write("Basic (with Sonic Adventure DX additions)");
									}
									else
									{
										sw.Write("Basic");
									}
									break;
								case ModelFormat.Chunk:
									sw.Write("Chunk");
									break;
								case ModelFormat.GC:
									sw.Write("GC");
									break;
							}
							sw.WriteLine(" model");
							sw.WriteLine(" * ");
							sw.WriteLine(" * Generated by DataToolbox");
							sw.WriteLine(" * ");
							if (modelFile != null)
							{
								if (!string.IsNullOrEmpty(modelFile.Description))
								{
									sw.Write(" * Description: ");
									sw.WriteLine(modelFile.Description);
									sw.WriteLine(" * ");
								}
								if (!string.IsNullOrEmpty(modelFile.Author))
								{
									sw.Write(" * Author: ");
									sw.WriteLine(modelFile.Author);
									sw.WriteLine(" * ");
								}
							}
							sw.WriteLine(" */");
							sw.WriteLine();
							var labels_m = new List<string> { model.Name };
							model.ToStructVariables(sw, basicDX, labels_m);
							foreach (var anim in animations)
							{
								anim.ToStructVariables(sw);
							}
							sw.Flush();
							sw.Close();
						}
					}
					else if (type == TextType.NJA)
					{
						outext = ".nja";
						var isDup = destination.ToLowerInvariant().Contains(".dup");
						if (destination == "")
						{
							destination = Path.Combine(Path.GetDirectoryName(source), Path.GetFileNameWithoutExtension(source) + outext);
						}
						if (!overwrite && File.Exists(destination))
						{
							while (File.Exists(destination))
							{
								destination = Path.Combine(Path.GetDirectoryName(destination), Path.GetFileNameWithoutExtension(destination) + "_" + outext);
							}
						}
						using (var sw2 = File.CreateText(destination))
						{
							string objtype = string.Empty;
							switch (model.GetModelFormat())
							{
								case ModelFormat.Basic:
								case ModelFormat.BasicDX:
								default:
									objtype = "Model";
									break;
								case ModelFormat.Chunk:
									objtype = "CnkModel";
									break;
								case ModelFormat.GC:
									objtype = "GjModel";
									break;
								case ModelFormat.XJ:
									objtype = "XjModel";
									break;
							}
							if (isNinja2)
							{
								sw2.WriteLine($"/* NJA 2.0.00 Ninja2AsciiObject {objtype} (SI) */");
								sw2.WriteLine("/* Generated by Data Toolbox */");
								sw2.WriteLine(Environment.NewLine + $"/* ROOT OBJECT : {model.Name} n({model.GetObjects().Length}) d(10) v({model.CountAllVertices()}) */");
							}
							else
							{
								sw2.WriteLine($"/* NJA 1.0.00 NinjaAsciiObject (SI) */");
								sw2.WriteLine("/* Generated by Data Toolbox */");
								sw2.WriteLine(Environment.NewLine + $"/* ROOT OBJECT : {model.Name} n({model.GetObjects().Length}) d(10) */");
							}
								var labels_nj = new List<string> { model.Name };
							model.ToNJA(sw2, labels_nj, null, isDup, isNinja2: isNinja2);
							sw2.Flush();
							sw2.Close();
						}
					}
					break;
				case ".saanim":
					var animation = NJS_MOTION.Load(source);
					if (type == TextType.CStructs)
					{
						outext = ".c";
						if (destination == "")
						{
							destination = Path.Combine(Path.GetDirectoryName(source), Path.GetFileNameWithoutExtension(source) + outext);
						}
						if (!overwrite && File.Exists(destination))
						{
							while (File.Exists(destination))
							{
								destination = destination = Path.Combine(Path.GetDirectoryName(destination), Path.GetFileNameWithoutExtension(destination) + "_" + outext);
							}
						}
						using (var sw = File.CreateText(destination))
						{
							sw.WriteLine("/* NINJA Motion");
							sw.WriteLine(" * ");
							sw.WriteLine(" * Generated by DataToolbox");
							sw.WriteLine(" * ");
							sw.WriteLine(" */");
							sw.WriteLine();
							animation.ToStructVariables(sw);
							sw.Flush();
							sw.Close();
						}
					}
					else if (type == TextType.JSON)
					{
						outext = ".json";
						if (destination == "")
						{
							destination = Path.Combine(Path.GetDirectoryName(source), Path.GetFileNameWithoutExtension(source) + outext);
						}
						if (!overwrite && File.Exists(destination))
						{
							while (File.Exists(destination))
							{
								destination = destination = Path.Combine(Path.GetDirectoryName(destination), Path.GetFileNameWithoutExtension(destination) + "_" + outext);
							}
						}
						var js = new JsonSerializer { Culture = CultureInfo.InvariantCulture };
						using (TextWriter tw = File.CreateText(destination))
						using (var jtw = new JsonTextWriter(tw) { Formatting = Formatting.Indented })
							js.Serialize(jtw, animation);
					}
					else if (type == TextType.NJA)
					{
						outext = animation.IsShapeMotion() ? ".nas" : ".nam";
						var isDum = destination.ToLowerInvariant().Contains(".dum");
						if (destination == "")
						{
							destination = Path.Combine(Path.GetDirectoryName(source), Path.GetFileNameWithoutExtension(source) + outext);
						}
						if (!overwrite && File.Exists(destination))
						{
							while (File.Exists(destination))
							{
								destination = destination = Path.Combine(Path.GetDirectoryName(destination), Path.GetFileNameWithoutExtension(destination) + "_" + outext);
							}
						}
						using (var sw2 = File.CreateText(destination))
						{
							string motiontype = "MOTION";
							if (isNinja2)
							{
								sw2.WriteLine($"/* NAM 2.0.00 Ninja2AsciiMotion (MAX) */");
								sw2.WriteLine("/* Generated by Data Toolbox */");
								if (animation.IsShapeMotion())
									motiontype = "SHAPE MOTION";
								if (animation.IsCameraMotion())
									motiontype = "CAMERA MOTION";
								sw2.WriteLine($"/* {motiontype} : {animation.Name} */");
							}
							else
							{
								sw2.WriteLine($"/* NAM 1.0.00 NinjaAsciiMotion (MAX) */");
								sw2.WriteLine("/* Generated by Data Toolbox */");
								if (animation.IsShapeMotion())
									motiontype = "SHAPE MOTION";
								if (animation.IsCameraMotion())
									motiontype = "CAMERA MOTION";
								sw2.WriteLine($"/* {motiontype} : {animation.Name} */");
							}
							animation.ToNJA(sw2, null, isDum, isNinja2: isNinja2);
							sw2.Flush();
							sw2.Close();
						}
					}
					break;
				case ".satex":
					var texlist = NJS_TEXLIST.Load(source);
					if (type == TextType.CStructs)
					{
						outext = ".c";
						if (destination == "")
						{
							destination = Path.Combine(Path.GetDirectoryName(source), Path.GetFileNameWithoutExtension(source) + outext);
						}
						if (!overwrite && File.Exists(destination))
						{
							while (File.Exists(destination))
							{
								destination = destination = Path.Combine(Path.GetDirectoryName(destination), Path.GetFileNameWithoutExtension(destination) + "_" + outext);
							}
						}
						using (var sw = File.CreateText(destination))
						{
							sw.WriteLine("/* NINJA Texlist");
							sw.WriteLine(" * ");
							sw.WriteLine(" * Generated by DataToolbox");
							sw.WriteLine(" * ");
							sw.WriteLine(" */");
							sw.WriteLine();
							texlist.ToStruct(sw);
							sw.Flush();
							sw.Close();
						}
					}
					else if (type == TextType.NJA)
					{
						outext = ".tls";
						if (destination == "")
						{
							destination = Path.Combine(Path.GetDirectoryName(source), Path.GetFileNameWithoutExtension(source) + outext);
						}
						if (!overwrite && File.Exists(destination))
						{
							while (File.Exists(destination))
							{
								destination = destination = Path.Combine(Path.GetDirectoryName(destination), Path.GetFileNameWithoutExtension(destination) + "_" + outext);
							}
						}
						using (var sw2 = File.CreateText(destination))
						{
							if (isNinja2)
							{
								sw2.WriteLine($"/* TLS 2.0.00 Ninja2AsciiTexlist (MAX) */");
							}
							else
							{
								sw2.WriteLine($"/* TLS 1.0.00 NinjaAsciiTexlist (MAX) */");
							}
							sw2.WriteLine("/* Generated by Data Toolbox */");
							sw2.WriteLine($"/* TEXLIST     : {texlist.Name} n({texlist.NumTextures}) */");
							texlist.ToNJA(sw2);
							sw2.Flush();
							sw2.Close();
						}
					}
					break;
			}
		}
	}
}